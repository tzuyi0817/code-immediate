name: CI-Pipeline

on:
  pull_request:
    types: [opened, synchronize, reopened]

  workflow_call:

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - name: Setup environment
        uses: './setup.yml'

      - name: Run unit test
        run: pnpm test:unit
